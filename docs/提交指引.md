# 最终提交指引（赛道 2）

> 截止：**2026-08-06 23:59（北京时间）**
> 评分：功能完整 60 分 + AMD Radeon GPU 本地推理/ROCm 优化 40 分

---

## 一、提交材料清单

| 材料 | 状态 | 说明 |
|---|---|---|
| 项目源码 | ✅ 完成 | `private-rag-agent/` 完整仓库 |
| README + 架构说明 | ✅ 完成 | 见 `README.md` |
| 演示视频 | 🎬 按脚本录制 | 见 `docs/演示视频脚本.md` |
| AMD 优化证据 | ⏳ 待 RC GPU | `rocm-smi` 截图 + `bench_amd.py` 数据 |
| 现场演示准备 | ✅ 可跑 | `python main.py ui` → localhost:7860 |

## 二、现场演示流程（推荐 3~4 分钟）

1. **开场**：打开 `localhost:7860`，展示空状态动画插画（QUERY→VECTOR DB→GROUNDED）
2. **导入文档**：点"导入文档"上传 1-2 个文件，展示"6 文档 / 15 片段"计数
3. **单 Agent 问答**：问"我的项目用了什么技术栈？"
   - 强调：右侧运行轨迹面板实时显示 `rag_search` 工具调用
   - 强调：回答带引用来源卡片，点开抽屉看原文高亮
4. **多 Agent 并行**：切"多 Agent 并行"模式，问"分析这个项目的技术栈、AMD优化点和部署方式"
   - 强调：计划面板 3 个子任务 + 进度条
   - 强调：3 个子 Agent 并行检索、事实核查卡片（grounding 分数）
   - 强调：汇总只依据通过核查的内容
5. **GPU 监控**：切到 GPU 面板，展示利用率/显存/温度曲线
6. **收尾**：强调"检索可追溯、回答可核查、数据不出本机"

## 三、多 Agent 模式演示小抄（背诵要点）

```
分解 → 并行研究 → 事实核查 → 综合

"为什么用多 Agent？复杂问题拆成 n 个独立子问题，
在 AMD GPU 上多个推理并发排队，串行转并行，
墙钟时间大幅下降——这是 AMD 加速的核心价值。"

"为什么核查？模型可能编造。我们把每句话和检索到的
原文做重叠度比对，不支撑的如实标注。看这个核查卡片，
grounding 0.5+ 就是已支撑，引用文件必须真实存在。"
```

## 四、AMD 40 分材料清单

在 Radeon Cloud 实例上采集（见 `RC_部署指引.md`）：

```bash
# 1. ROCm 环境证据
rocminfo | head -30

# 2. GPU 实时监控截图
rocm-smi

# 3. 模型 100% GPU offload 证据
ollama ps

# 4. 推理吞吐对比（CPU vs GPU）
python benchmarks/bench_amd.py

# 5. vLLM 推理服务验证
curl http://localhost:8000/v1/models
```

将以上截图与数据放入 `benchmarks/` 目录，并在 README 更新实测对比表。

## 五、提交前检查清单

- [ ] `python main.py ui` 可正常启动
- [ ] 知识库已导入演示文档（6 文档 / 15 片段）
- [ ] 单 Agent / 多 Agent 两种模式都跑通
- [ ] 引用抽屉高亮正常
- [ ] GPU 监控面板有数据（演示模式也可）
- [ ] README 已更新 AMD 实测数据
- [ ] 演示视频已录（1080P，3-4 分钟）
- [ ] 源码打包无调试残留（`tools/rc/archive/` 已 gitignore）
