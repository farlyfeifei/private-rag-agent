# 本地私有 RAG Agent 依赖
# 安装: pip install -r requirements.txt

# --- 推理后端（三选一按需） ---
ollama>=0.3.0                # 本地 Ollama 服务（默认，Windows/Linux 通用）
openai>=1.40.0              # vLLM / OpenAI 兼容接口（Radeon Cloud 工作区用）
# llama-cpp-python          # AMD ROCm 版: CMAKE_ARGS="-DGGML_HIP=ON" pip install llama-cpp-python

# --- RAG / 向量 ---
chromadb>=0.5.0             # 本地向量库
rank-bm25>=0.2.2            # BM25 混合检索（无 GPU 依赖）
pypdf>=4.0.0                # PDF 解析
python-docx>=1.1.0          # Word 解析
openpyxl>=3.1.0             # Excel 解析
python-pptx>=0.6.23         # PPT 解析
sentence-transformers>=2.2.0 # cross-encoder 重排 + 备用 embedding

# --- UI（自研 FastAPI + SSE，原生 HTML/CSS/JS 前端） ---
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
# gradio>=4.0.0            # 旧版 UI 依赖（新前端不再需要）

# --- 配置 / 工具 ---
pyyaml>=6.0
tiktoken>=0.7.0

# --- AMD 优化监控 ---
# pyamdgpuinfo                # AMD GPU 显存/利用率监控（ROCm 环境）
# rocm-smi                    # AMD 系统工具（单独安装）
